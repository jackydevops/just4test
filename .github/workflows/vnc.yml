name: ubuntu-vnc

on:
  workflow_dispatch:

jobs:
  vnc:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Install desktop + VNC
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq xfce4 xterm x11vnc novnc websockify dbus-x11

      - name: Start VNC + tunnel
        run: |
          Xvfb :1 -screen 0 1920x1080x24 &
          sleep 2
          x11vnc -display :1 -forever -nopw -rfbport 5901 -quiet &
          DISPLAY=:1 startxfce4 &
          websockify --web /usr/share/novnc 6080 localhost:5901 &
          curl -sL -o cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x cloudflared
          ./cloudflared tunnel --url http://localhost:6080 > tunnel.log 2>&1 &

      - name: Keep alive
        run: |
          for i in $(seq 1 90); do
            URL=$(grep -o 'https://[a-z0-9.-]*\.trycloudflare\.com' tunnel.log | head -1)
            [ -n "$URL" ] && break
            sleep 2
          done
          echo "VNC: $URL/vnc.html"
          echo "::notice title=VNC::$URL/vnc.html"
          while true; do sleep 60; done
